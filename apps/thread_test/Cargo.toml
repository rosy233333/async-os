[package]
name = "thread_test"
version = "0.1.0"
edition = "2021"

[dependencies]
async_std = { path = "../../modules/async_std", features = [
    "irq",
    "multitask",
    "thread-api",
] }
log = "0.4"

与调试、panic展开相关：codex resume 019fd4cd-d502-76d0-ac23-deb0149dfc78

最新的二进制有行号，而且调试信息是完整的。检查过程如下：

  1. 先确认哪个是最新构建的 ELF

  find . -path ./target -prune -o -name '*.elf' -printf '%T@ %p\n' | sort -rn

  结果最新的是 apps/coroutine_test/coroutine_test_riscv64-qemu-virt.elf，其余（shell、echoserver、thread_test 等）都是更早
  的构建。

  2. 检查段表里有没有行号相关段

  readelf -S apps/coroutine_test/coroutine_test_riscv64-qemu-virt.elf | rg -i "debug|symtab|eh_frame"

  结果包含 .debug_line（行号表）、.debug_info、.debug_abbrev、.debug_aranges、.debug_ranges、.debug_str，以及 .symtab（符
  号表）。file 也确认：with debug_info, not stripped。

  3. 功能验证——用 addr2line 真的解析出一个源码行号

  先取一个已知函数地址（正好就是上次讨论的 slow_path_entry）：

  readelf -sW <elf> | rg "slow_path_entry"
  # ffffffc08021a94e  420 FUNC  trampoline::arch::riscv::slow_path_entry

  再解析函数内部几个地址：

  riscv64-unknown-elf-addr2line -e <elf> -f -C ffffffc08021a94e ffffffc08021a960 ffffffc08021a9d0

  - ...a94e（函数入口）→ trampoline::arch::riscv::slow_path_entry，行号为 ?（入口地址归属 CGU 占位文件，属正常现象）
  - ...a960 → core::sync::atomic::atomic_load at .../core/src/sync/atomic.rs:3905（标准库源码行号，解析正常）
  - ...a9d0 → trampoline::arch::riscv::slow_path_entry at /home/rosy/桌面/async-os/modules/trampoline/src/arch/riscv/
    mod.rs:123——对照当前源码，123 行正是 117 行那个异常 panic 分支里 current_task().id_name() 的参数求值处

  最后一个结果很关键：它说明这份二进制不仅带着行号表，而且行号与当前源码精确对应（panic 分支的位置都对得上）。

  结论：这次最新构建的 coroutine_test 二进制是带 debuginfo 构建的（release 默认不带，所以这份产物应该是用 debug 模式或额外
  传了 -C debuginfo 构建的）；而仓库里更早的某些 ELF（比如 vdso_scheduler_ktest 的 kernel_test）只有符号表、没有 .debug_*
  段，即没有行号。